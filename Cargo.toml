[package]
name = "engram"
version = "0.1.0"
edition = "2021"
description = "Garbage-collected memory for Claude agents"

[dependencies]
clap = { version = "4", features = ["derive"] }
rusqlite = { version = "0.32", features = ["bundled"] }
chrono = "0.4"
ratatui = "0.29"
crossterm = "0.28"
